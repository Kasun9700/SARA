{
  "name": "home-design-landing",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "build": "nuxi build",
    "start": "node .output/server/index.mjs",
    "generate": "nuxi generate",
    "deploy": "gh-pages -d .output/public"
  },
  "dependencies": {
    "nuxt": "^3.8.0"
  }
}
